#!/bin/bash

bars=(▁ ▂ ▃ ▄ ▅ ▆ ▇ █)
WIDTH=20   # 👈 ancho FIJO (muy importante)

while read -r line; do
  out=""
  values=(${line//;/ })
  for ((i=0; i<WIDTH; i++)); do
    v=${values[i]:-0}
    out+="${bars[$v]}"
  done
  echo "$out"
done < /tmp/cava.fifo


